steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - admin/Dockerfile
      - --build-arg
      - VITE_API_URL=${_VITE_API_URL}
      - -t
      - ${_IMAGE}
      - admin
images:
  - ${_IMAGE}
